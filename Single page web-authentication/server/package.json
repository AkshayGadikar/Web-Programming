{
  "name": "auth",
  "version": "1.0.0",
  "description": "In order to facilitate easy grading and testing, the specs are buggy:\r the timeout for an authentication token is set when the token is\r created and not affected when the token is used.  A more practical\r specification would have the timeout being reset each time the token\r is used, so that the timeout only applies to inactivity.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^1.0.3",
    "crypto-js": "^3.1.9-1",
    "express": "^4.16.2",
    "minimist": "^1.2.0",
    "mongodb": "^2.2.33"
  }
}
